import React from 'react'
import MaterialPages from '../Page/MaterialPages'

export const MaterialPage = ({ materialData }) => {
  return (
    <>
        <MaterialPages materialData={materialData} />
    </>
  )
}

export default MaterialPage;